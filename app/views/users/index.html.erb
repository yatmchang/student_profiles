<div class="container">
  <h1>All Students</h1>

  <div>
    <% @users.each do |user| %>
      <%#= image_tag user.avatar.url(:large) %>
      <%= user.full_name %>
      <%= button_to "View Profile", user_path(user), :class => "btn btn-default", :method => :get  %>
    <% end %>
  </div>
</div>
