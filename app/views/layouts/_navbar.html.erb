
<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a href="/" class="navbar-brand"><%= image_tag("codecorelogo.png") %></a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li class="link"><%= link_to "Available Students", available_path %></li>
        <li class="link"><%= link_to "All Students", users_path %></li>
      </ul>
      <% if current_page?(controller: 'users', action: 'index') || current_page?(controller: 'users', action: 'available')%>
        <form class="nav navbar-nav navbar-form navbar-left" role="search">
          <div class="form-group link">
            <input type="text" id="search-form" class="form-control" placeholder="Search...">
          </div>
        </form>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.full_name %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "View Profile", user_path(current_user) %></li>
            <li><%= link_to "Edit Profile", edit_user_path(current_user) %></li>
            <% if current_user.is_admin? %>
            <li><%= link_to "Admin Page", admin_path%></li>
            <% end %>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Sign Out", sessions_path, method: :delete %></li>
        <% else %>
            <li class="link"><%= link_to "Sign In", new_session_path %></li>
            <li class="link"><%= link_to "Sign Up", new_user_path %></li>
        <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
